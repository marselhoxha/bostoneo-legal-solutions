# CREATE INDEX CONCURRENTLY cannot run inside an explicit transaction.
# Disable Flyway's default per-migration transaction wrap so the
# CONCURRENTLY statements in V58 execute in their own implicit transactions.
executeInTransaction=false
