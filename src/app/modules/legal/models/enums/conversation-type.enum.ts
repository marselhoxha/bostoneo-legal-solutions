export enum ConversationType {
  Question = 'question',
  Draft = 'draft',
  Summarize = 'summarize',
  Upload = 'upload',
  Workflow = 'workflow'
}

export enum TaskType {
  LegalQuestion = 'LEGAL_QUESTION',
  GenerateDraft = 'GENERATE_DRAFT',
  SummarizeCase = 'SUMMARIZE_CASE',
  AnalyzeDocument = 'ANALYZE_DOCUMENT'
}

export enum ResearchMode {
  Fast = 'FAST',
  Thorough = 'THOROUGH'
}
